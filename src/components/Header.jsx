import React from 'react';

const Header = () => {
    return (
        <header className="header">
            <h1>Custom Dashboard Panel</h1>
            <nav>
                <a href="#">Home</a>
                <a href="#">Features</a>
                <a href="#">Settings</a>
            </nav>
        </header>
    );
};

export default Header;
